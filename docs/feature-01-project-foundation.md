# Feature F001: Project Foundation

## Goal

Create the base repo structure for frontend, backend, docs, and AI-ready project memory.

## Scope

- Monorepo folder structure
- Next.js frontend app
- FastAPI backend app
- Environment examples
- Root README
- `.claude` project files

## Acceptance Criteria

- `frontend` folder exists and Next.js app can start.
- `backend` folder exists and FastAPI app can start.
- Root docs exist.
- `.claude` files exist.
- No appointment feature required yet.

## Implementation Checklist

- [ ] Create root folder.
- [ ] Create Next.js app inside `frontend`.
- [ ] Create FastAPI app inside `backend`.
- [ ] Create backend `.venv`.
- [ ] Update `.gitignore`.
- [ ] Update root `README.md`.
- [ ] Add `.env.example` files.
- [ ] Add docs and `.claude` specs.


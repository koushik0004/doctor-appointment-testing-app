# Report Index

| Report | Area | 5-line Summary | When to Read Full Report |
|---|---|---|---|
| `docs/reports/feature-3/scheduling-verification-report.md` | Booking availability verification | Verifies the booking flow after removing the dependency on persisted availability rows.<br>Confirms future dates stay selectable and today hides elapsed slots.<br>Confirms booked slots disappear after booking and do not affect other doctors.<br>Includes live browser verification against local frontend/backend targets.<br>Records test/build commands used during verification. | Read the full report when validating appointment-slot behavior, investigating calendar regressions, or checking the exact verification scenarios and command history. |
| `docs/reports/feature-3/scheduling-refactor-plan.md` | Booking availability refactor plan | Explains why the old booking flow broke when static seeded availability expired.<br>Maps the old frontend and backend dependency chain around `doctor_availability` rows.<br>Identifies root cause, impacted files, deletable functions, and required remaining tables.<br>Recommends moving to generated schedule-based availability instead of stored slot rows.<br>Acts as the design rationale for the scheduling refactor. | Read the full report when changing availability generation, appointment validation, schema dependencies, or when you need the full file-by-file refactor plan. |
| `docs/reports/feature-4/feature-4-backend-validation-report.md` | Appointment APIs validation | Validates backend booking, appointment details, and search endpoints as of June 19, 2026.<br>Confirms `POST /api/appointments`, `GET /api/appointments/{id}`, and search endpoints work.<br>Includes representative request/response payloads and tested edge cases.<br>Notes duplicate booking, past-slot, invalid-input, and missing-parameter handling.<br>Records pytest coverage and the observed deprecation warning. | Read the full report when modifying appointment API contracts, debugging search behavior, or needing exact sample payloads and edge-case expectations. |
| `docs/reports/feature-5-ai-chat/feature-report-2026-06-21-ai-chat-phase1.md` | AI chat phase 1 | Documents the initial reusable AI widget and backend chat endpoint integration.<br>Covers global widget mounting, service abstraction, and plain text chat request flow.<br>Explains the first rule-based backend doctor knowledge layer with no LLM usage.<br>Lists the key frontend/backend modules introduced for chat.<br>Captures current limitations such as no streaming or persisted history. | Read the full report when working on base chat widget architecture, initial chat API behavior, or understanding how the first frontend/backend chat integration was structured. |
| `docs/reports/feature-6-intent-recognition/feature-report-ai-chat-phase2-2026-06-21.md` | AI chat phase 2 intent recognition | Covers the upgrade from plain text chat to deterministic intent-based structured responses.<br>Explains doctor cards, availability cards, and booking CTA navigation into `/appointments?doctorId=...`.<br>Details the backend intent detector and the normalized chat response contract.<br>Includes commit-by-commit analysis for both backend and frontend chat rendering changes.<br>This is a large implementation report with architectural flow and integration detail. | Read the full report when changing chat intent routing, structured widget rendering, or appointment handoff behavior from chat. |
| `docs/reports/feature-7-intent-with-entity-extraction/feature-report-ai-chat-phase3-2026-06-21.md` | AI chat phase 3 entity extraction | Documents natural-language entity extraction for specialization, gender, fee, location, date, and time.<br>Explains how backend search filters feed doctor discovery and availability results.<br>Covers frontend rendering of search summaries, doctor cards, and availability cards.<br>Notes the time-preference routing fix so messages like `after 5 PM` hit availability search.<br>This is the largest report and contains the deepest architecture and commit analysis. | Read the full report when changing chat search parsing, entity extraction rules, structured filter rendering, or time-based availability interpretation. |
| `docs/reports/feature-8-chatbot-fix/report-faq-enhancements.md` | AI chat FAQ enhancements | Documents deterministic FAQ improvements for booking help, cancellation guidance, and fee-question clarification.<br>Adds the `CANCEL_APPOINTMENT_HELP` intent and structured `help_steps` payload support.<br>Explains backend priority changes that prevent fee questions from being misrouted to booking help.<br>Covers the frontend reuse of the existing appointment-help card for ordered guidance steps.<br>Includes commit-by-commit analysis and targeted regression coverage notes. | Read the full report when changing FAQ-style chat responses, help-step rendering, cancellation guidance, or intent-priority ordering for support queries. |
| `docs/reports/feature-10/architecture/phase-02-review.md` | Phase 02 orchestration review | Reviews the single-entry-point chat orchestration changes introduced in Phase 02.<br>Enumerates new files, contracts, service boundaries, and API changes without modifying code.<br>Confirms there were no database changes and maps the end-to-end dependency flow.<br>Identifies request-scoped conversation metadata that is active versus fields that are currently unused.<br>Calls out speculative abstractions that appear over-engineered for the current deterministic prototype. | Read the full report when evaluating the current chat orchestration architecture, deciding whether to simplify Phase 02 scaffolding, or planning a next phase that adds persistence or alternate AI engines. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-03-report.md` | AI chat phase 03 workflow engine | Documents the request-scoped Workflow Engine introduced under the Conversation Manager.<br>Covers booking, cancellation, and appointment confirmation execution through existing appointment services.<br>Includes the June 29 stabilization fixes for doctor-profile routing, sticky booking continuation, and confirmation-page handoff from chat.<br>Explains the chat contract additions for workflow draft/result metadata while keeping endpoints unchanged.<br>Summarizes design choices, current limitations, and the next recommended phase. | Read the full report when changing chat workflow execution, workflow metadata contracts, doctor-profile routing, multi-turn booking continuation, or the frontend handoff from chat into the confirmation flow. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-04-vectorless-rag-impl.md` | AI chat phase 04 Vector-less RAG implementation | Documents the repository-local knowledge layer, deterministic retrieval service, and conversation-manager integration added in the July 1-2, 2026 commit range.<br>Covers the additive `knowledge_source` chat contract and the frontend footer rendering for knowledge-backed text replies.<br>Now includes stabilization summaries for the July 2-3 fixes: routing precedence, retrieval ranking, entity-extraction false positives, booking-workflow continuation regressions, and booking validation failures that previously surfaced as HTTP 503/500.<br>Lists added and modified files across backend knowledge loading, chat orchestration, widget mapping, tests, and supporting docs.<br>Explains compatibility boundaries and the current limitations of the deterministic single-document retrieval approach. | Read the full report when changing the Phase 04 knowledge repository, retrieval ranking, knowledge-backed chat routing, booking-workflow error normalization, frontend source-footer rendering, or the regression coverage added around FAQ retrieval and workflow coexistence. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-04-manual-test-checklist.md` | AI chat phase 04 manual test support | Provides a comprehensive manual QA checklist for the Vector-less RAG prototype.<br>Covers positive, negative, edge, regression, workflow, and existing chatbot scenarios.<br>Checks knowledge-backed replies, source footer rendering, and fallback behavior.<br>Verifies workflow responses still win over knowledge retrieval when active.<br>Serves as the main human-testing guide for the phase-4 prototype. | Read the full report when running manual QA on the Vector-less RAG chat prototype or when validating regressions after changes to knowledge retrieval or chat rendering. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-llm-integration-architecture.md` | AI chat phase 06 LLM integration seam | Documents the Phase 6.1 provider-neutral LLM Integration boundary added under `backend/app/llm/`.<br>Covers the internal message/request/response contracts, provider/registry protocols, and inactive integration facade.<br>Explicitly records that there is still no provider SDK, no provider-specific code, and no runtime wiring.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, and Prompt Builder changes.<br>Acts as the bridge document for later provider-adapter phases. | Read the full report when adding provider adapters, deciding where an LLM caller should live, or verifying the intended boundaries of the inactive LLM seam. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-provider-abstraction.md` | AI chat phase 06 provider abstraction | Documents the Phase 6.2 refinement of the inactive LLM seam around provider translation and registry behavior.<br>Covers the canonical translator protocols, shared base adapter pipeline, inactive in-memory registry, and default-provider resolution rules.<br>Explicitly records that there is still no SDK integration, no concrete provider implementation, and no runtime wiring.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, and Prompt Builder changes.<br>Acts as the handoff document for later concrete provider-adapter implementation phases. | Read the full report when implementing a concrete provider adapter, extending registry behavior, or verifying how provider-native payloads must remain encapsulated inside the inactive `app.llm` boundary. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-canonical-llm-contract.md` | AI chat phase 06 canonical LLM contract | Documents the Phase 6.3 refinement of the inactive LLM seam around its canonical request and response models.<br>Covers provider-neutral support for future structured output, tool calling, reasoning, streaming, citation, model/provider metadata, and multimodal request intent.<br>Explicitly records that there is still no SDK integration, no concrete provider implementation, and no runtime wiring.<br>Confirms backward compatibility plus zero behavior, API, frontend, database, workflow, retrieval, and Prompt Builder changes.<br>Acts as the handoff document for later phases that implement concrete adapters against the canonical contract. | Read the full report when extending canonical LLM request/response models, mapping provider-native output back into canonical metadata, or verifying which future-capability fields already exist in the inactive `app.llm` contract. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-generation-orchestration.md` | AI chat phase 06 generation orchestration | Documents the Phase 6.4 addition of an inactive orchestration layer on top of the Prompt Builder and provider-neutral LLM seam.<br>Covers the orchestration request/result models, Prompt Builder delegation, canonical request construction, LLM service delegation, and normalized result shaping.<br>Explicitly records that there is still no SDK integration, no concrete provider implementation, and no runtime wiring.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, and Prompt Builder changes.<br>Acts as the handoff document for later phases that may connect a guarded runtime caller to the inactive orchestrator. | Read the full report when extending the inactive prompt-to-generation composition flow, verifying orchestration boundaries, or deciding how future runtime code should call the LLM seam without bypassing Prompt Builder or provider abstraction. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-provider-configuration.md` | AI chat phase 06 provider configuration | Documents the Phase 6.5 addition of an inactive provider-neutral configuration layer under `backend/app/llm/`.<br>Covers canonical configuration models, nested environment mapping, deterministic defaults, provider validation rules, and `.env.example` documentation.<br>Explicitly records that there is still no SDK integration, no provider client instantiation, and no runtime wiring.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and orchestrator changes.<br>Acts as the handoff document for later phases that may instantiate adapters and registries from configuration. | Read the full report when extending provider configuration fields, changing env-variable mapping, deciding how future adapter setup should consume configuration, or verifying the intended inactive boundary of the new config seam. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-generation-budget-architecture.md` | AI chat phase 06 generation budget architecture | Documents the Phase 6.5.5 addition of an inactive provider-neutral generation-budget seam under `backend/app/llm/`.<br>Covers canonical budget profiles, reasoning/token/latency/quality/cost controls, configuration-backed profile overrides, and the adapter-facing budget translation contract.<br>Explicitly records that there is still no SDK integration, no provider-native parameter mapping in upstream code, and no runtime wiring.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and orchestrator changes.<br>Acts as the handoff document for later phases that translate canonical budget intent inside concrete adapters. | Read the full report when extending generation-budget fields, changing default profiles or override rules, deciding how future adapters should consume canonical budget intent, or verifying the intended inactive boundary of the new budget seam. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-concrete-provider-adapters.md` | AI chat phase 06 concrete provider adapters | Documents the Phase 6.6 addition of inactive concrete provider adapters under `backend/app/llm/providers.py`.<br>Covers config-backed adapter classes for OpenAI, Claude, Gemini, OpenRouter, and Ollama plus explicit transport injection and adapter-factory setup.<br>Explicitly records that there is still no SDK integration, no live API calling, and no runtime wiring.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and orchestrator changes.<br>Acts as the handoff document for later phases that attach real provider clients in a guarded runtime path. | Read the full report when changing concrete adapter translation logic, adding a new provider adapter, deciding how configuration should instantiate adapters, or verifying the intended inactive boundary of the new concrete adapter layer. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-runtime-composition-dependency-assembly.md` | AI chat phase 06 runtime composition | Documents the Phase 6.7 addition of an inactive runtime composition root under `backend/app/llm/composition.py`.<br>Covers one-time configuration loading, separate transport creation, enabled-provider adapter instantiation, explicit registry ownership, and constructor-only service/orchestrator composition.<br>Explicitly records that there is still no SDK integration, no live API calling, and no chatbot/runtime activation.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and business-logic changes.<br>Acts as the handoff document for later phases that may attach real transports or guarded runtime activation. | Read the full report when wiring the inactive LLM subsystem into a startup graph, changing transport-construction strategy, refining dependency ownership, or verifying how the subsystem stays inactive after composition. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-runtime-activation-feature-flags.md` | AI chat phase 06 runtime activation | Documents the Phase 6.8 addition of an inactive runtime activation seam under `backend/app/llm/activation.py`.<br>Covers top-level runtime feature flags, provider-readiness evaluation, canonical activation diagnostics, and safe failure handling around invalid configuration or dependency assembly.<br>Explicitly records that there is still no SDK integration, no live API calling, and no chat-routing activation.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and business-logic changes.<br>Acts as the handoff document for later phases that may wire guarded runtime routing or shadow-mode execution. | Read the full report when changing activation-policy rules, extending readiness diagnostics, wiring future safe-startup evaluation, or verifying how activation remains isolated from routing and provider execution. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-execution-policy-runtime-routing.md` | AI chat phase 06 execution policy | Documents the Phase 6.9 addition of an inactive execution-policy seam under `backend/app/llm/execution_policy.py`.<br>Covers canonical execution modes, ownership and fallback decisions, activation-aware routing, and composed policy-service assembly.<br>Explicitly records that there is still no SDK integration, no live API calling, and no chat-runtime routing change.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and business-logic changes.<br>Acts as the handoff document for later phases that may wire guarded runtime routing or hidden shadow execution. | Read the full report when changing runtime-routing policy rules, extending execution modes or fallback behavior, wiring future activation-aware callers, or verifying how execution decisions remain isolated from the live chat path. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-06-production-readiness-operational-excellence.md` | AI chat phase 06 operational readiness | Documents the Phase 6.10 addition of an inactive operational-readiness seam under `backend/app/llm/operations.py`.<br>Covers canonical observability, accounting, health, retry, timeout, audit, privacy, rollout, and performance models plus the composed readiness service.<br>Explicitly records that there is still no SDK integration, no live API calling, and no production rollout or runtime activation change.<br>Confirms zero behavior, API, frontend, database, workflow, retrieval, Prompt Builder, and business-logic changes.<br>Acts as the handoff document for later phases that may wire production telemetry, health dashboards, rollout policy, or runtime transport policy. | Read the full report when changing operational telemetry models, extending health/retry/timeout policy, planning production rollout controls, or verifying how operational readiness remains isolated from the live chat path. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-07-shadow-mode-integration.md` | AI chat phase 07 shadow mode | Documents the Phase 7.2 wiring of hidden shadow execution through `backend/app/llm/facade.py` and the live `ConversationManager` path.<br>Covers the runtime sequence from official response selection through shadow policy evaluation, background orchestration, diagnostics capture, and LLM-output discard.<br>Explains the safety guarantees that preserve workflow, knowledge, deterministic fallback, API contracts, and frontend behavior.<br>Lists the provider-neutral observability and audit models reused for shadow diagnostics plus the rollback strategy.<br>Includes the modified-file set and the validation commands that confirmed zero user-visible behavior changes. | Read the full report when changing shadow-mode triggering, facade-owned diagnostics, live chat integration with the LLM facade, or the safety rules that keep shadow execution invisible to end users. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-07-llm-e2e-pipeline.md` | AI chat phase 07 prompt-to-LLM shadow pipeline | Documents the Phase 7.3 completion of the shadow prompt path from runtime inputs through the Prompt Builder, Prompt Renderer, orchestrator, provider request, diagnostics, and response discard.<br>Explains the concrete gap closed in this phase: workflow context now renders into the final prompt instead of stopping at canonical context creation only.<br>Confirms the runtime still uses the existing facade, composition root, activation, execution policy, and provider-neutral orchestrator with no API or visible-chat changes.<br>Lists the focused prompt-builder and runtime-facade regression coverage that proves rendered prompts reach the provider path.<br>Records the modified files and validation commands for the phase. | Read the full report when changing prompt assembly, rendered shadow prompts, Prompt Builder ownership of prompt creation, or the end-to-end path from runtime shadow inputs into the provider request. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-07-controlled-runtime-generation.md` | AI chat phase 07 controlled runtime generation | Documents the Phase 7.4 policy-gated runtime-generation path added to `backend/app/llm/facade.py`.<br>Explains how the facade now evaluates the existing execution policy, reuses the Prompt Builder and orchestrator, and only returns generated runtime results when an LLM-capable mode is explicitly allowed.<br>Confirms default deterministic behavior remains unchanged and generation failures fall back safely without exposing provider errors.<br>Lists the focused facade and policy regression coverage that proves enabled, disabled, failing, and deterministic paths behave as intended.<br>Records the modified files and validation commands for the phase. | Read the full report when changing controlled runtime generation, facade policy gating, deterministic fallback behavior, or the runtime boundary between visible chat and guarded LLM execution. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-07-runtime-response-validation.md` | AI chat phase 07 runtime response validation | Documents the Phase 7.5 provider-neutral validation gate added to `backend/app/llm/facade.py`.<br>Explains how controlled generation now passes through canonical response validation before any future visible LLM output and falls back safely when responses are empty, whitespace-only, malformed, or otherwise invalid.<br>Confirms validation operates only on canonical orchestration results and does not inspect provider-native payloads, workflow state, or Prompt Builder internals.<br>Lists the focused validator and facade regression coverage that proves valid, invalid, and fallback paths behave as intended.<br>Records the modified files and validation commands for the phase. | Read the full report when changing runtime-response validation, facade quality gating, deterministic fallback behavior, or the canonical response boundary between provider output and visible chat. |
| `docs/reports/feature-10/ai-impl-architecture-and-implementation/phase-07-controlled-user-visible-llm-responses.md` | AI chat phase 07 controlled user-visible LLM responses | Documents the Phase 7.6 provider-neutral eligibility gate added to `backend/app/llm/eligibility.py` and wired through `backend/app/llm/facade.py`.<br>Explains how validated LLM output is exposed only for explicitly approved low-risk conversational scenarios while workflow-owned and other non-approved requests remain deterministic.<br>Confirms eligibility operates only on canonical execution-policy and orchestration metadata after successful validation and does not inspect provider-native payloads.<br>Lists the focused eligibility and facade regression coverage that proves approved, rejected, validation-failure, and deterministic paths behave as intended.<br>Records the modified files and validation commands for the phase. | Read the full report when changing runtime-response eligibility, user-visible LLM gating, deterministic fallback behavior, or the canonical response boundary between validated output and user-visible chat. |
| `docs/reports/test-data-report.md` | Demo data seeding | Records the July 3, 2026 manual-test data seeding run against `backend/app.db`.<br>Now includes four additional doctor demo profiles plus duplicate-safe booking checks against already-seeded appointments.<br>Confirms the seed utility pins its default SQLite target to the backend DB, uses the service layer for bookings, and respects the 10-row cap.<br>Notes that legacy `doctor_availability` rows were intentionally left untouched.<br>Captures remaining demo-data gaps for future manual QA setup. | Read the full report when refreshing demo data, verifying whether local seed rows already exist, or checking exactly what doctor and booking demo rows were inserted. |
| `docs/reports/test-data-health-report.md` | Demo data health audit | Records the July 3, 2026 zero-write health audit of `backend/app.db`.<br>Confirms current doctors, patients, appointments, and generated availability behavior remain valid for implemented workflows.<br>Documents retained legacy `doctor_availability` issues, including one unsupported historical appointment-type row.<br>Captures the explicit no-data-loss decision to avoid mutating legacy or ambiguous records.<br>Lists current row counts and recommended follow-up actions. | Read the full report when validating whether local demo data is still safe after schema or workflow changes, or before performing any cleanup of legacy availability rows. |

## Warnings

- `docs/reports/feature-6-intent-recognition/feature-report-ai-chat-phase2-2026-06-21.md` is large and should be read only for chat-intent or structured-rendering work.
- `docs/reports/feature-7-intent-with-entity-extraction/feature-report-ai-chat-phase3-2026-06-21.md` is very large and should be read only for entity-extraction or natural-language search changes.
- `docs/reports/feature-8-chatbot-fix/report-faq-enhancements.md` is moderately large; prefer the summary above unless the task is specifically about FAQ routing or help-step responses.

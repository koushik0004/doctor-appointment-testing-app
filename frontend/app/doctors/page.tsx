export default function DoctorsPage() {
  return (
    <section className="rounded-3xl border border-slate-200 bg-white p-8 shadow-soft">
      <span className="inline-flex rounded-full bg-brand-50 px-3 py-1 text-sm font-medium text-brand-900">
        Doctors route
      </span>
      <h1 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
        Doctors listing foundation
      </h1>
      <p className="mt-4 max-w-2xl text-base text-slate-600">
        This route is reserved for the doctor listing feature. It currently
        provides the shared page shell and styling only.
      </p>
    </section>
  );
}
